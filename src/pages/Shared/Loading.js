import React from 'react';

const Loading = () => {
    return (
        <div>
            <p className='text-xl font-bold text-warning'>Loading....</p>
        </div>
    );
};

export default Loading;